name: farmacia_mobile
description: App de monitoreo para farmacia.
publish_to: 'none'

# AGREGA ESTO AQUÍ:
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0
  provider: ^6.0.5
  firebase_core: ^4.9.0
  firebase_messaging: ^16.2.2
  flutter_local_notifications: ^21.0.0
  mobile_scanner: ^3.0.0
  # ... otras dependencias que tengas

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0